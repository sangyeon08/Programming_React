# Programming 수업 소스코드 📝
## 02_counter
vanilla HTML, JavaScript
## REACT 시작 🤒
```shell
npm create vite@latest .
```

HMR : 코드 수정 시 브라우저 전체 새로고침 없이, 변경된 모듈만 즉시 업데이트하여 반영하는 기능!

### CounterApp 🥰
- `useState()`
- `onClick = {() => set함수()}`
- `onClick = {() => set함수((이전state) => 이전state +1)}`


### TodoListApp 🌸
- React Component 분리
- for - htmlFor, class -> classNAme
- props
- `<input id ={id} value = {}`, `<label htmlFor={}>`>
- onChange, onClick, onKeyUp
- 구조 분해 할당, `...스프레드 연산자`
- `<form on Submit\{}><form>`
- `map()` : RD, `filter()` : D
- `<TodoItem key = {}/>`
- `const handleEvent = (event) => {}`
- `{조건식 ? 참 : 거짓}`
- `{조건식 && 참}`
- `{!조건식 && 거짓}`
- LocalStorage, `useEffect`